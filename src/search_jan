#!/bin/sh
# ================================================================
# コンテナ内のシェルから `search_jan` コマンドとして使うためのパーサーです。
# コンテナの /usr/local/bin に設置して利用します。
# ================================================================

/usr/local/bin/php /app/index.php "$@"
